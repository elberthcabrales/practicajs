const longestPalindrome = function (s) {
  const result = '';
  if (s.length < 1 || s.length > 1000) return s;
  for (let i = 0; i < s.length; i++) {

  }

  return result;
};

const input1 = 'babad';
const output1 = 'bab';
console.log(output1 === longestPalindrome(input1));

const input2 = 'cbbd';
const output2 = 'bb';
